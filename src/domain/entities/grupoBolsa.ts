export default interface GrupoBolsa {
    id: number,
    nome: string,
    remuneracao: number,
    quantidadeTotal: number,
    quantidadeRestatnte: number,
    dataInicio: Date,
    dataFim: Date
}